---
// src/components/Icon.astro
const {
  name,
  size = 24,
  title = "",
  color = "var(--clr-primary)",
} = Astro.props;

import IconReadPost from "./icons/IconReadPost.astro";
import IconArrow from "./icons/IconArrow.astro";
// ...add more as needed

const icons = {
  readpost: IconReadPost,
  arrow: IconArrow,
};

const SelectedIcon = icons[name.toLowerCase()];
---

{
  SelectedIcon ? (
    <SelectedIcon
      style={`width: ${size}px; height: ${size}px; color: ${color};`}
      title={title}
    />
  ) : (
    <span style="color: var(--clr-accent);">Missing icon: {name}</span>
  )
}
